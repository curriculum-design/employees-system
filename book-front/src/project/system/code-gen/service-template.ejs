import BaseService from '@/utils/BaseService'
class <%= serviceName %> extends BaseService {
    constructor() {
        super('<%= apiName %>')
    }
}
const target = new <%= serviceName %>()
export default target.proxy()
